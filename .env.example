# ===========================================
# PRODUCTION ENVIRONMENT VARIABLES (COOLIFY)
# ===========================================

# Node Environment
NODE_ENV=production

# Database (PostgreSQL)
# Coolify will provide a DATABASE_URL automatically if you link a PostgreSQL service
DATABASE_URL=postgresql://user:password@host:5432/database

# Auth Secret - REQUIRED
# Generate a secure secret: openssl rand -base64 32
NUXT_SESSION_PASSWORD=your-secret-key-here-min-32-chars-CHANGE-THIS-IN-PRODUCTION

# Site Configuration
SITE_NAME=Marie Leroy
SITE_URL=https://your-domain.com

# File Upload
# In production, this should be a persistent volume in Coolify
UPLOAD_DIR=/app/uploads

# Email Configuration (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Analytics (optional)
GOOGLE_ANALYTICS_ID=
GOOGLE_TAG_MANAGER_ID=

# ===========================================
# DEVELOPMENT ENVIRONMENT VARIABLES (LOCAL)
# ===========================================

# For local development with Docker Compose:
# POSTGRES_PASSWORD=changeme_production_password
# DATABASE_URL=postgresql://admin:changeme_production_password@postgres:5432/marieleroy
# NUXT_SESSION_PASSWORD=your-dev-secret-at-least-32-chars
# SITE_URL=http://localhost:3000
