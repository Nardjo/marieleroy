# Enable ALL build scripts (required for pnpm 10.x security model)
# Prisma needs this to download engine binaries during install
# Note: Docker builds use --dangerously-allow-all-builds flag instead
enable-scripts=true
dangerously-allow-all-builds=true
